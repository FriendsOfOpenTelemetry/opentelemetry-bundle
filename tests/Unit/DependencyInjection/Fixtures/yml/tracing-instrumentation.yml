open_telemetry:
  service:
    namespace: 'FriendsOfOpenTelemetry/OpenTelemetry'
    name: 'Test'
    version: '0.0.0'
    environment: test
  instrumentation:
    cache:
      tracing:
        enabled: true
    console:
      tracing:
        enabled: true
    doctrine:
      tracing:
        enabled: true
    http_client:
      tracing:
        enabled: true
    http_kernel:
      tracing:
        enabled: true
    mailer:
      tracing:
        enabled: true
    messenger:
      tracing:
        enabled: true
    twig:
      tracing:
        enabled: true
  traces:
    tracers:
      main:
        provider: default
    providers:
      default:
        type: default
        sampler: always_on
        processors:
          - simple
    processors:
      simple:
        type: simple
        exporter: otlp
    exporters:
      otlp:
        dsn: http+otlp://localhost
  metrics:
    meters:
      main:
        provider: noop
  logs:
    loggers:
      main:
        provider: noop
